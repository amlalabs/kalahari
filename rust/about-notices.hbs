# Third-Party Notices

This document lists the third-party dependencies of the `amla` Rust workspace
and their license terms. Generated by `cargo about generate about-notices.hbs`.

---

## Bundled Model

### potion-base-2M

- Repository: https://huggingface.co/minishlab/potion-base-2M
- License: MIT
- Copyright: Minish Lab

The potion-base-2M model (~8 MB) is bundled in `amla-sandbox-shell` for text
embeddings.

---

## Rust Crate Dependencies

Each section below covers one license; the **License** line gives the
SPDX identifier and the bullet list enumerates the crates that ship under
it. Headings use the comma-joined crate names to keep them unique
(markdownlint MD024).

{{#each licenses}}
### {{#each used_by}}{{#unless @first}}, {{/unless}}{{crate.name}} v{{crate.version}}{{/each}} ({{{name}}})

Used by:
{{#each used_by}}
- [{{crate.name}} v{{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```text
{{{text}}}
```

---

{{/each}}
